Mutation:
    type: object
    config:
        fields:
            bookmark:
                type: "Bookmark!"
                resolve: "@=mutation('bookmark', [args['url'], args['title'], args['id']])"
                args:
                    id:
                        type: "Int"
                        defaultValue: null
                    title:
                        type: "String!"
            collection:
                type: "Collection!"
                resolve: "@=mutation('collection', [args['title'], args['id']])"
                args:
                    id:
                        type: "Int"
                        defaultValue: null
                    title:
                        type: "String!"
