{% block user_autocomplete_widget %}
    <user-autocomplete :name="'{{ form.vars.full_name }}'" :endpoint="'{{ path(form.vars.autocomplete_path, form.vars.autocomplete_params) }}'"></user-autocomplete>
{% endblock %}
